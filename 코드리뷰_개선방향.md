# 마왕 토벌 — 코드 리뷰 & 개선 방향

전체 코드(약 9,200줄, JS 14개 파일 + CSS + HTML)를 검토한 결과입니다. 게임이 이미 상당히 완성도 높게 동작하고 있다는 점을 먼저 말씀드리고 싶어요. 아래는 "지금 당장 고장난 것"보다는 **앞으로 기능을 추가할 때 버그가 덜 생기고, 유지보수가 쉬워지는 방향**에 초점을 맞춘 제안입니다.

우선순위는 ⭐(높음) → 🔹(중간) → ▫️(낮음/취향)으로 표시했어요.

---

## 1. 저장/불러오기 구조 — 가장 중요 ⭐

### 문제
지금까지 우리가 함께 고친 버그 중 상당수가 여기서 나왔어요. `SaveManager._serializePlayer()`가 저장할 필드를 **약 50개 손으로 일일이 나열**하는 방식이라, Player에 새 속성을 추가할 때마다 이 목록에도 똑같이 추가하는 걸 잊으면 "불러오면 그 값만 초기화되는" 버그가 조용히 생깁니다. (인트로 대화가 리셋되던 그 버그가 정확히 이거였죠.)

또한 `hydrate()`에서 `Object.assign(p, raw)`로 한 번 통째로 복사한 뒤, 같은 필드들(party2, cooldowns, status, affinity 등)을 아래에서 `raw.X || 기본값`으로 **다시 한 번 덮어쓰는** 중복이 많아요. 읽기 어렵고, 두 곳이 어긋날 위험이 있습니다.

### 개선 방향
**핵심 아이디어: "저장 안 할 것"만 지정하고 나머지는 전부 자동 저장.**

```js
// 저장에서 제외할 휘발성 필드만 명시 (화면 좌표, 임시 전투 상태 등)
const TRANSIENT_FIELDS = new Set([
  "pixelX", "pixelY", "_introChainActive", "guardBuff",
  // ... 저장할 필요 없는 것만
]);

_serializePlayer(p) {
  const out = {};
  for (const key of Object.keys(p)) {
    if (TRANSIENT_FIELDS.has(key)) continue;
    if (typeof p[key] === "function") continue;
    out[key] = p[key];
  }
  // getter(_money 등)는 별도 처리
  out.money = p.money;
  return out;
}
```

이렇게 하면 **Player에 새 필드를 추가해도 저장 목록을 건드릴 필요가 없어요.** 앞으로 같은 종류의 버그가 원천적으로 사라집니다.

`hydrate()`도 "구조가 있는 필드(객체/배열)에 기본값 병합"만 남기고, 단순 플래그는 `Object.assign`에 맡기면 절반으로 줄어듭니다.

### 추가 제안
- **저장 버전 마이그레이션 함수 분리**: 지금 `hydrate()` 안에 `warrior→knight` 변환, `metVillageChief→introChainDone` 보정 등이 섞여 있어요. `migrateSave(raw, fromVersion)` 같은 별도 함수로 빼면 나중에 v4, v5로 올릴 때 깔끔합니다.
- **저장 실패 대비**: localStorage가 꽉 차거나(용량 초과) 사파리 시크릿 모드일 때 `setItem`이 예외를 던집니다. 지금도 `try/catch`는 있지만, 실패 시 사용자에게 "저장 공간이 부족합니다" 같은 안내를 보여주면 더 좋아요.

---

## 2. 대화 체인의 setInterval 폴링 ⭐

### 문제
마을 대화가 `setInterval`로 "대화창이 닫혔나?"를 250~400ms마다 확인하는 방식이에요. town-scene.js에만 이 패턴이 **13곳** 있습니다. 우리가 고친 버그들(대화 두 번 겹침, 불러오기 후 멈춤, 출석 팝업 충돌)이 전부 이 구조에서 나왔어요. 타이머가 살아남거나, 타이밍이 미묘하게 어긋나거나, 여러 타이머가 동시에 도는 게 근본 원인이었죠.

### 개선 방향
**대화가 "닫힐 때 콜백을 부르는" 방식으로 바꾸기.** 폴링을 없애는 게 핵심이에요.

```js
showNpcDialogue(npcId, onClose) {
  // ... 대화창 생성 ...
  const closeBox = () => {
    box.remove();
    if (onClose) onClose();   // ← 닫힐 때 직접 호출. 폴링 불필요
  };
}
```

지금은 `showNpcDialogue`를 부른 쪽에서 setInterval로 닫힘을 "감시"하는데, 대화창 자신이 닫힐 때 다음 단계를 호출하게 하면 타이밍 문제가 통째로 사라집니다. 인트로 체인도 콜백 중첩(또는 async/await + Promise)으로 깔끔하게 표현할 수 있어요:

```js
async function playIntroChain() {
  await showDialogue("self_intro_town");
  await showDialogue("merchant");
  await showDialogue("self_after_merchant");
  await showDialogue("village_chief");
  // ...
}
```

`showDialogue`가 "닫히면 resolve되는 Promise"를 반환하게만 하면, 13개의 setInterval이 전부 사라지고 위처럼 위에서 아래로 읽히는 코드가 됩니다. 이건 리팩터링 규모가 좀 있지만, **앞으로 대화 관련 버그를 거의 0으로 만들 수 있는** 가장 효과적인 변경이에요.

---

## 3. 중복된 초상화/이미지 경로 매핑 🔹

### 문제
캐릭터 초상화 경로 맵(`{knight: "images/sd_knight.png", healer: ...}`)이 거의 똑같은 형태로 **여러 곳에 16번 넘게** 반복 정의돼 있어요. `images/Silvia_front.png`는 town-scene.js에서만 37번 등장합니다. 새 캐릭터를 추가하거나 이미지 파일명을 바꾸면 이 모든 곳을 찾아 고쳐야 해요.

### 개선 방향
**한 곳에 모아두고 참조하기.** models.js나 새 `assets.js`에 중앙 정의:

```js
const PORTRAITS = {
  knight: "images/portrait_Knight.png",
  mage:   "images/portrait_magician.png",
  // ...
};
const SD_SPRITES = {
  knight: "images/sd_knight.png",
  // ...
};
const NPC_PORTRAITS = {
  merchant: "images/sd_merchant.png",
  princess: "images/Silvia_front.png",
  // ...
};
```

그리고 각 파일에서 이 전역 객체를 참조하면, 이미지 교체가 한 줄 수정으로 끝납니다. 이미 `item-icons.js`에서 `ITEM_ICON_IMAGES`로 잘 하고 있는 방식이라, 같은 패턴을 캐릭터에도 확장하는 거예요.

---

## 4. 인라인 스타일 vs CSS 🔹

### 문제
JS 안에 `style="..."` 문자열이 124곳, `.cssText = "..."`가 19곳 있어요. 디자인을 바꾸려면 CSS가 아니라 JS 코드를 뒤져야 하고, 같은 스타일이 여기저기 복붙돼 있습니다.

### 개선 방향
반복되는 UI 요소(슬롯 카드, 모달, 버튼)는 CSS 클래스로 빼는 걸 권합니다. 예를 들어 우리가 만든 불러오기 슬롯 카드의 인라인 스타일을 `.load-slot-card` 클래스로 만들면, 나중에 모양을 바꿀 때 CSS 한 곳만 고치면 돼요. 동적인 값(진행도 막대 너비 등)만 인라인으로 남기고요.

`!important`가 7곳 있는데, 이건 보통 CSS 우선순위가 꼬였다는 신호예요. 셀렉터 구조를 정리하면 대부분 없앨 수 있습니다.

---

## 5. 파일 크기 — town-scene.js가 너무 큼 🔹

### 문제
town-scene.js가 **2,390줄**로 가장 큽니다. 마을 렌더링, 상점, 대장간, 은행, 퀘스트, 공주 대화, 동료 모집, 저장 슬롯 UI가 전부 한 파일에 있어요. 특정 기능을 찾거나 고칠 때 스크롤이 많이 필요하죠.

### 개선 방향
기능 단위로 파일을 나누는 걸 고려해보세요. 이미 `card-game.js`, `trap-room.js`처럼 prototype 확장 패턴(`TownScene.prototype.X = ...`)을 쓰고 계시니, 같은 방식으로:
- `town-shop.js` — 상점/대장간 로직
- `town-bank.js` — 은행/투자
- `town-dialogue.js` — NPC 대화 + 인트로 체인
- `town-party.js` — 동료 모집/호감도

이렇게 나누면 각 파일이 300~500줄로 관리하기 편해집니다. 동작은 그대로 두고 파일만 쪼개는 거라 위험도 낮아요.

---

## 6. 전역 네임스페이스 ▫️

### 현황
30개 넘는 클래스/상수가 `window.X`로 전역에 올라가 있어요. 지금 규모에선 큰 문제는 아니지만, 이름 충돌 위험과 의존 관계 파악이 어려운 면이 있습니다.

### 개선 방향 (선택)
당장은 괜찮습니다. 다만 나중에 규모가 더 커지면 하나의 네임스페이스 객체(`window.RPG = { Player, SaveManager, ... }`)로 묶거나, 빌드 도구(Vite 등)를 도입해 ES 모듈(`import`/`export`)로 전환하는 걸 고려해볼 수 있어요. 이건 큰 작업이라 지금 단계에선 권하지 않습니다.

---

## 7. 잘 되어 있는 점 👍

비판만 적으면 균형이 안 맞으니, 좋은 부분도 짚어둘게요.
- **`money` getter/setter로 골드 상한을 자동 클램프**하는 설계는 깔끔합니다. 모든 골드 변동에 자동 적용돼서 버그 여지가 적어요.
- **`createItemId()`가 crypto.randomUUID 우선, 폴백 제공**하는 것도 견고합니다.
- **아이템 아이콘 시스템(`item-icons.js`)의 키 폴백 구조**(`sword_legend` → `sword` → 벡터)는 확장성이 좋아요. 위 3번에서 제안한 캐릭터 이미지도 이 패턴을 따르면 됩니다.
- **서비스 워커 캐시 정리 로직**이 valid 목록 기반으로 잘 짜여 있어서, 버전만 올리면 구버전이 자동 삭제됩니다.
- prototype 확장으로 **기능을 여러 파일에 나누는 패턴**을 이미 쓰고 계신 것도 좋아요. 5번 제안이 자연스럽게 들어맞습니다.

---

## 우선순위 요약

| 순위 | 항목 | 효과 | 작업량 |
|------|------|------|--------|
| ⭐ 1 | 저장 화이트리스트 → 블랙리스트 방식 | 저장 관련 버그 원천 차단 | 중 |
| ⭐ 2 | 대화 폴링 → 콜백/Promise | 대화 버그 원천 차단 | 중~대 |
| 🔹 3 | 이미지 경로 중앙화 | 유지보수 편의 | 소 |
| 🔹 4 | 인라인 스타일 → CSS 클래스 | 디자인 수정 편의 | 중 |
| 🔹 5 | town-scene.js 파일 분리 | 코드 탐색 편의 | 소~중 |
| ▫️ 6 | 전역 네임스페이스 정리 | 장기 확장성 | 대 |

---

**가장 추천하는 시작점**은 **1번(저장 구조)**입니다. 작업량 대비 효과가 가장 크고, 지금까지 가장 자주 버그가 났던 지점이라서요. 원하시면 1번부터 실제로 코드를 고쳐드릴게요. 어느 항목부터 진행할지 말씀해 주세요.
